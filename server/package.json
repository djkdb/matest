{
  "name": "exam-master-push",
  "private": true,
  "type": "module",
  "description": "시험 마스터 웹푸시 발송 서버 (Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen-keys": "node scripts/generate-vapid.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "webpush-webcrypto": "^1.0.5"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
